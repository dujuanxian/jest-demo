{
  "scripts": {
    "test": "jest",
    "test:log": "jest --json --outputFile=test.log"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "jest-junit": "^6.4.0"
  },
  "jest": {
    "coverageReporters": [
      "text"
    ],
    "reporters": [
      "default",
      "jest-junit"
    ]
  },
  "jest-junit": {
    "output": "test/junit.xml",
    "usePathForSuiteName": "true"
  }
}
